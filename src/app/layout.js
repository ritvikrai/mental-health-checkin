import './globals.css'
export const metadata = { title: 'Wellness Check-in', description: 'Your daily mental health companion' }
export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>
}
